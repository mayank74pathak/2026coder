   string largestOddNumber(string num) {
        int end=num.size()-1;
        while(end>=0)
        {
          if(!(num[end]&1)){
            end--;
          }
        else
        break;
        }
    return num.substr(0,end+1);
    }
